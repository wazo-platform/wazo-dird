server {
    listen 127.0.0.1:9489;

    location / {
         include fastcgi_params;
         fastcgi_param PATH_INFO $fastcgi_script_name;
         fastcgi_param SCRIPT_NAME "";
         fastcgi_pass unix:/var/run/xivo-dird/xivo-dird.sock;
    }
}
