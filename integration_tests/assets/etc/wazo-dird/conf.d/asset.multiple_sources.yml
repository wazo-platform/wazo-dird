enabled_plugins:
  backends:
    unknown: true  # unknown backends should not prevent wazo-dird from starting
  services:
    unknown: true  # unknown backends should not prevent wazo-dird from starting
  views:
    unknown: true  # unknown backends should not prevent wazo-dird from starting

bus:
  enabled: false
