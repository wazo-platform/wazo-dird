type: xivo
name: xivo-{{ uuid }}
searched_columns:
  - firstname
  - lastname
first_matched_columns:
  - exten
confd_config:
  host: {{ hostname }}
  port: {{ port }}
  version: "1.1"
  https: false
  verify_certificate: false
format_columns:
  number: "{exten}"
